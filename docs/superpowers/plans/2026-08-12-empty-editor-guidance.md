# Initial Scene Template Implementation Plan

**Goal:** Start every new script in `SCENE ONE`, with the caret at its end.

1. [x] Make multi-act and one-act document factories populate their first
   Scene with `SCENE ONE`.
2. [x] Keep the untouched templates eligible for autofocus and resolve the
   initial selection at the end of that Scene.
3. [x] Remove the R2 canvas and sidebar guidance, and cover the absence of
   extra canvas guidance with a browser test.
4. [ ] Run targeted browser tests, typechecks, lint, whitespace validation,
   and update the code graph.
