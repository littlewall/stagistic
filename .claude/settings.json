{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f && case \"$f\" in *.ts|*.tsx|*.js|*.jsx|*.mjs|*.cjs) d=$(dirname \"$f\"); while [ ! -f \"$d/package.json\" ] && [ \"$d\" != \"/\" ]; do d=$(dirname \"$d\"); done; cd \"$d\" && npx eslint --fix \"$f\" 2>/dev/null || true;; *.css|*.scss) npx stylelint --fix \"$f\" 2>/dev/null || true;; esac; }",
            "statusMessage": "Linting...",
            "timeout": 30
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "/Users/milanzitka/.local/bin/graphify hook-guard search"
          }
        ]
      },
      {
        "matcher": "Read|Glob",
        "hooks": [
          {
            "type": "command",
            "command": "/Users/milanzitka/.local/bin/graphify hook-guard read"
          }
        ]
      }
    ]
  }
}
