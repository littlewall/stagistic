# yaml-language-server: $schema=https://moonrepo.dev/schemas/project.json
$schema: 'https://moonrepo.dev/schemas/project.json'

# Config-only package with no scripts today -> opt out of all inherited tasks.
workspace:
  inheritedTasks:
    exclude: ['lint', 'typecheck', 'test']
