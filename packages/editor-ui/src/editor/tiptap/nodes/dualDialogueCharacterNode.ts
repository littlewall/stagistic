import {ELEMENT_DUAL_DIALOGUE_CHARACTER} from '@stagistic/script-core';

import {createFountainNode} from './createFountainNode';

export const DualDialogueCharacterNode = createFountainNode({
    name: 'dualDialogueCharacter',
    legacyType: ELEMENT_DUAL_DIALOGUE_CHARACTER,
});
