import {
    Title,
} from '@mantine/core';

const AppDashboard = () => {
    return (
        <>
            <Title order={2} mb="md">Dashboard</Title>
        </>
    );
};

export default AppDashboard;
