import {
    Title,
} from '@mantine/core';

const AppSettings = () => {
    return (
        <>
            <Title order={2} mb="md">Settings</Title>
        </>
    );
};

export default AppSettings;
